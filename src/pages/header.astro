
<header >
        <div class="logo">
            <img src='../../llave-izq.png' alt="">Angel <span>.dev</span><img class="right-key" src='../../llave-izq.png' alt="">
        </div>
        
        <div class="bars__menu">
                <span class="line1__bars-menu"></span>
                <span class="line2__bars-menu"></span>
                <span class="line3__bars-menu"></span>
        </div>

        <nav id="nav">
            <ul>
                <li class="li"><a href="#inicio">Inicio</a></li>
                <li class="li"> <a href="#habilidades">Habilidades</a></li>
                <li class="li"> <a href="#proyectos">Proyectos</a></li>
            </ul>
    
            <ul>
                <li><a href="../../cv.pdf" target="_blank"  class="cv">CV</a></li>
                <li><a href="https://www.linkedin.com/in/%C3%A1ngel-valent%C3%ADn-altieri-a319a0256/" target="_blank" data-tooltip="Ver mi Linkedin"><i class="bi bi-linkedin"></i></a></li>
                <li><a href="https://github.com/angelvalentinn" target="_blank"  data-tooltip="Ver mi Github"><i class="bi bi-github"></i></a></li>
            </ul>
        </nav>
    
</header>

<style>
    header {
        position:fixed;
        right: 0;
        left: 0;
        top: 0;
        z-index: 100;
        text-align: center;
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem;
        background-color: #1b1b1f;

        
        .logo {
            display: flex;
            align-items: center;
            -webkit-animation: fade-in-fwd 1s both;
            animation: fade-in-fwd 1s both;

            .right-key {
                transform:rotateY(180deg);
            }
            span {
                color: var(--clr-primary);
            }
        }

        .bars__menu{
            width: 30px;
            height: 30px;
            transform: scale(.8);
            cursor: pointer;
        }

        nav {
            transform: translateX(100%);
            transition: .3s ease transform;
            position: fixed;
            right: 0;
            top: 60px;
            bottom: 0;
            left: 30%;
            background-color: #1b1b1f;
            -webkit-animation: fade-in-fwd 1s both;
            animation: fade-in-fwd 1s both;
            
            flex-direction: column;
            align-items: center;
            justify-content: space-evenly;
            color: var(--clr-white-text);
            
            ul {
                display: flex;
                flex-direction: column;
                gap: 2rem;

                &:first-child  {
                    letter-spacing: 2px;
                    color: var(--clr-primary);

                    li:first-child a {
                        color: var(--clr-primary);
                    }
                }

                &:nth-child(2) {
                    flex-direction: row;
                    align-items: center;

                    li {
                        font-size: 1.5rem;
                        &:first-child {
                            font-size: 1rem;
                            padding: .3rem;
                            border-radius: 8px;
                            border: 2px solid var(--clr-primary);
                        }
                    }

                    .bi-linkedin {
                        color: #0a66c2;
                    }
                }

            }
        }

        .animate-bars {
            transform: translateX(0);
        }

        .disabled {
            display: flex;

        }

        .bars__menu span {
            display: block;
            width: 100%;
            height: 2px;
            background: var(--clr-primary);
            margin-top: 6px;
            transform-origin: 0px 100%;
            transition: all 300ms;
        }

        .activeline1__bars-menu{
            transform: rotate(45deg) translate(-2px, 1px); 
        }

        .activeline2__bars-menu{
            opacity: 0;
            margin-left: -30px; 
        }

        .activeline3__bars-menu{
            transform: rotate(-45deg) translate(-4px, 2px);
        }

    }

    @keyframes blur {
        to {
            backdrop-filter: blur(10px);
            background-color: #20202061;
            box-shadow: 0 4px 8px #ffffff1a;
        }
    }

    @media screen and (width > 750px) {
        header {
            background-color: inherit;
            position:fixed;
            right: 0;
            left: 0;
            top: 0;
            z-index: 100;
            text-align: center;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 1rem 2rem;
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: space-between;
            animation: blur linear both;
            animation-timeline: scroll();
            animation-range: 0 500px;
            z-index: 999999999;
            

            .logo {
                font-size: 1.4rem;
                span {
                    color: var(--clr-primary);
                }
            }

            .bars__menu{
                display: none;
            }

            nav {
                padding-right: 2rem;
                transform: none;
                position: initial;
                display: flex;
                flex-direction: row;
                gap: 2rem;
                background-color: transparent;

                ul {
                    display: flex;
                    flex-direction: row;
                    gap: 1rem;

                    &:first-child  {
                        letter-spacing: 1px;
                        color: inherit;

                        li:first-child a {
                            color: inherit;

                            &:hover {
                                color: var(--clr-primary);
                            }
                        }


                    }

                    &:nth-child(2) {
                        flex-direction: row;
                        align-items: center;

                        li {
                            cursor: pointer;
                            font-size: 1.5rem;
                            &:first-child {
                                font-size: 1rem;
                                padding: .3rem;
                                border: 2px solid var(--clr-primary);
                                transition: .2s ease all;

                                &:hover {
                                    background-color: var(--clr-primary);
                                    color: var(--clr-white-text);
                                }
                            }

                            


                        }
                    }

                    li {

                        a {
                            transition: .2s ease color;
                            &:hover {
                                color: var(--clr-primary);
                            }


                        }
                        .cv:hover {
                            color: var(--clr-white-text);
                        }
                    }
                }
            }

        .animate-bars {
            transform: translateX(0);
        }

        .disabled {
            display: flex;

        }

        .bars__menu span {
            display: block;
            width: 100%;
            height: 2px;
            background: var(--clr-primary);
            margin-top: 6px;
            transform-origin: 0px 100%;
            transition: all 300ms;
        }

        .activeline1__bars-menu{
            transform: rotate(45deg) translate(-2px, 1px); 
        }

        .activeline2__bars-menu{
            opacity: 0;
            margin-left: -30px; 
        }

        .activeline3__bars-menu{
            transform: rotate(-45deg) translate(-4px, 2px);
        }

    }
    }

</style>

<script>
    const bars = document.querySelector(".bars__menu");
    const nav = document.getElementById("nav");
    const line1__bars = document.querySelector(".line1__bars-menu");
    const line2__bars = document.querySelector(".line2__bars-menu");
    const line3__bars = document.querySelector(".line3__bars-menu");
    const lis = document.querySelectorAll('.li');

    bars.addEventListener("click", () => {
        animateBars();
        nav.classList.toggle("animate-bars");
        nav.classList.toggle("disabled");
    });

    lis.forEach(li => {
        li.addEventListener('click', () => {
            animateBars();
            nav.classList.toggle("animate-bars");
            nav.classList.toggle("disabled");
        })
    })

    function animateBars(){
        line1__bars.classList.toggle("activeline1__bars-menu");
        line2__bars.classList.toggle("activeline2__bars-menu");
        line3__bars.classList.toggle("activeline3__bars-menu");
    }


</script>