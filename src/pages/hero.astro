
<section id="inicio">
    <article class="first">
        <img src="../../me.webp" alt="Foto de Ángel Valentín Altieri">
        <h2>Ángel Valentín Altieri</h2>
        <p><i class="bi bi-geo-alt-fill"></i>Argentina, Buenos Aires</p>
    </article>

    <article class="second">
        <h1>Estudiante de Sistemas</h1>
        <div class="paragraphs">
            <p>
                Tengo 21 años y soy estudiante de tercer año de Licenciatura en Sistemas. Durante mi formación académica aprendí tecnologías como C, Java y MySQL. De forma paralela y por mi cuenta  <strong>aprendí a crear sitios web dinámicos</strong>, adquiriendo habilidades en HTML, CSS, JavaScript, React y Astro.  <strong>Estoy motivado a seguir aprendiendo</strong> y aplicar mis conocimientos para superar nuevos desafíos en el ámbito tecnológico.
            </p>
        </div>
    </article>
</section>

<style>
    section {
        margin-top: 6rem;
        padding-inline: 1rem;
        display: flex;
        align-items: center;
        flex-direction: column;
        gap: 3rem;
        -webkit-animation: fade-in-fwd 2s both;
	    animation: fade-in-fwd 2s both;

        
        article {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .first {
            gap: .5rem;
            img {
                height: 170px;
                width: 170px;
                object-fit: cover;
                filter: drop-shadow(0px 0px 6px  #ffffff2a);
                z-index: -10;
            }
            
            h2 {
                margin-top: .6rem;
                font-size: 1.6rem;
                color: var(--clr-primary);
                font-family: 'Dancing Script', sans-serif;
            }

            p {
                margin-top: 0;
                font-size: 0.9rem;
                font-weight: 200;
                i {
                    color: var(--clr-primary);
                    margin-right: 8px;
                    font-weight: 300;
                }
                
            }
        }

        .second {
            display: flex;
                flex-direction: column;
                align-items: flex-start;
            h1 {
                font-size: 1.4rem;
                text-wrap: pretty;
                font-weight: bold;
                background: linear-gradient(var(--clr-primary), var(--clr-terciary));
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
            }

        }
    }


    @media screen and (width > 550px) {
        section {
            
            .first {
                gap: .5rem;

                img {
                    height: 200px;
                    width: 200px;
                }
                
                h2 {
                    font-size: 2rem;
                }

            }

            .second {

                h1 {
                    font-size: 1.9rem;
                    text-wrap: pretty;
                    font-weight: bold;
                    background: linear-gradient(var(--clr-primary), var(--clr-terciary));
                    -webkit-background-clip: text;
                    -webkit-text-fill-color: transparent;
                }
            }
    }
    }

    @media screen and (width > 800px) {
        section {
            margin-top: 2rem;
            display: grid;
            min-height: 100vh;
            grid-template-columns: 600px max-content;
            justify-content: center;
            gap: 0;

            .first {
                align-items: flex-start;
                position: relative;
                &::before {
                    content: '';
                    position: absolute;
                    left: -15px;
                    top: 0;
                    bottom: 0;
                    border: 1px solid #dddddd1d;
                }
            }

            .second {
                order: -1;
                width: 95%;
                h1 {
                    font-size: 2rem;
                }

                p {
                    text-wrap: pretty;
                }
            }
        }
    }

</style>